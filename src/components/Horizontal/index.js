/* eslint-disable-next-line no-unused-vars */
import React from 'react';
import * as Local from './styles';

export default function Horizontal(props) {
  return (
    <Local.HorizontalContainer>
      <Local.HorizontalInner />
    </Local.HorizontalContainer>
  );
}
